{
  "name": "aeiou-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/copy-data.mjs && node scripts/check-design.mjs && node scripts/check-content.mjs && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.2.0"
  }
}
